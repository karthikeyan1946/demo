<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
  </head>
  <body>
    <h3>Edit</h3>
    <form action="/products/<%= p.id %>?_method=PATCH" method="post">
        <label for="name">Product name:</label>
      <input
        type="text"
        name="name"
        id="name"
        placeholder="productname"
        value="<%= p.name %>"
      />
      <br>
      <label for="price">Product Price:</label>
      <input
        type="number"
        name="price"
        id="price"
        placeholder="price"
        value="<%= p.price %>"
      />
      <br>
      <label for="category">product category:</label>
      <select name="category" id="category">
        <% categories.map((c)=>{ %>
        <option value="<%= c %>" <%= c === p.category ? 'selected':'' %>><%= c %></option>
        <% }) %>

      </select>
      <br>
      <button>submit</button>
    </form>
    <a href="/products/<%= p.id %>/show">back to product details</a>
  </body>
</html>
