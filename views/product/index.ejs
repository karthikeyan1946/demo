<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>our products</title>
  </head>
  <body>
    <h1>product list</h1>
    <div>
      <ul>
        <% p.map( (i)=>{ %>
        <li><a href="/products/<%= i.id %>/show"><%= i.name %></a></li>

        <% } ) %>
      </ul>
      <a href="/">Home</a>
    </div>
  </body>
</html>
